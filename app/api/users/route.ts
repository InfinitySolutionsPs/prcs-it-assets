import {asc,eq} from "drizzle-orm";import {getDb} from "../../../db";import {appUsers} from "../../../db/schema";import {requirePermission} from "../../../lib/auth";
const clean=(u:typeof appUsers.$inferSelect)=>({...u,permissions:JSON.parse(u.permissions)as string[]});
export async function GET(request:Request){if(!await requirePermission(request,"users"))return Response.json({error:"غير مصرح"},{status:403});const db=await getDb();return Response.json({users:(await db.select().from(appUsers).orderBy(asc(appUsers.id))).map(clean)})}
export async function POST(request:Request){if(!await requirePermission(request,"users"))return Response.json({error:"غير مصرح"},{status:403});try{const p=await request.json()as{email?:string;fullName?:string;role?:string;permissions?:string[]};if(!p.email?.trim()||!p.fullName?.trim())return Response.json({error:"الاسم والبريد الإلكتروني مطلوبان"},{status:400});const db=await getDb(),[user]=await db.insert(appUsers).values({email:p.email.trim().toLowerCase(),fullName:p.fullName.trim(),role:p.role||"مستخدم",permissions:JSON.stringify(p.permissions||[])}).returning();return Response.json({user:clean(user)},{status:201})}catch(e){return Response.json({error:e instanceof Error?e.message:"تعذر إضافة المستخدم"},{status:500})}}
export async function PATCH(request:Request){if(!await requirePermission(request,"users"))return Response.json({error:"غير مصرح"},{status:403});try{const p=await request.json()as{id?:number;fullName?:string;role?:string;permissions?:string[];active?:boolean};const db=await getDb(),[user]=await db.update(appUsers).set({fullName:p.fullName?.trim(),role:p.role,permissions:JSON.stringify(p.permissions||[]),active:p.active}).where(eq(appUsers.id,Number(p.id))).returning();return Response.json({user:clean(user)})}catch(e){return Response.json({error:e instanceof Error?e.message:"تعذر تعديل المستخدم"},{status:500})}}
export async function DELETE(request:Request){const actor=await requirePermission(request,"users");if(!actor)return Response.json({error:"غير مصرح"},{status:403});const p=await request.json()as{id?:number};if(actor.id===Number(p.id))return Response.json({error:"لا يمكنك حذف حسابك الحالي"},{status:409});const db=await getDb();await db.delete(appUsers).where(eq(appUsers.id,Number(p.id)));return Response.json({deleted:true})}
